[package]
name = "soundtouch-sys"
version = "0.1.0"
edition = "2021"
description = "FFI bindings to SoundTouch (pitch shift, via bundled SoundTouchDLL C API)"
links = "soundtouch"
build = "build.rs"

[build-dependencies]
cc = "1"
